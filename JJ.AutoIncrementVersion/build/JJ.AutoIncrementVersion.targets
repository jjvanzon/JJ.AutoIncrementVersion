<Project TreatAsLocalProperty="
         ProjDir;PackDir;XmlPath;DirPropsPath;BeforeTargets;
         BuildNumWasMissing;BuildNumWasZero;DirPropsExists;BuildNumNew;
         DllProd;DllDev;Dll;
         DirPropsInnerContent;DirPropsContent;DirPropsReadOut;
         XmlReadOut;VariablesMessage;XmlContent">
    
  <!-- VARS -->

  <PropertyGroup>
          <ProjDir>$(MSBuildProjectDirectory)/</ProjDir>
          <PackDir>$(MSBuildThisFileDirectory)</PackDir>
          <XmlPath>$(ProjDir)../BuildNum.xml</XmlPath>
     <DirPropsPath>$(ProjDir)../Directory.Build.props</DirPropsPath>
    <BeforeTargets>BeforeBuild;BuildOnlySettings;PreBuildEvent;Restore;BuildGenerateSources;Pack</BeforeTargets>
    <BuildNumWasMissing>False</BuildNumWasMissing>
    <BuildNumWasMissing Condition="'$(BuildNum)'==''">True</BuildNumWasMissing>
    <BuildNumWasZero>False</BuildNumWasZero>
    <BuildNumWasZero Condition="'$(BuildNum)'=='0'">True</BuildNumWasZero>
    <DirPropsExists>False</DirPropsExists>
    <DirPropsExists Condition="Exists('$(DirPropsPath)')">False</DirPropsExists>
  </PropertyGroup>

  <!-- DLL -->

  <!--
  <PropertyGroup>
    <DllProd>$(PackDir)JJ.AutoIncrementVersion.Tasks.dll</DllProd>
    <DllDev>$(MSBuildThisFileDirectory)../../JJ.AutoIncrementVersion.Tasks/bin/$(Configuration)/netstandard2.0/JJ.AutoIncrementVersion.Tasks.dll</DllDev>
    <Dll Condition="Exists('$(DllProd)')">$(DllProd)</Dll>
    <Dll Condition="Exists('$(DllDev)')">$(DllDev)</Dll>
  </PropertyGroup>
  <UsingTask TaskName="JJ.AutoIncrementVersion.Tasks.WriteLinesNoTimestamp" AssemblyFile="$(Dll)" Condition="'$(Dll)'!=''" />
  -->
  
  <!-- DIR PROPS -->

  <Target Name="JJ_AutoInc_Init" BeforeTargets="$(BeforeTargets)">

    <!-- Build DirProps content -->
    <PropertyGroup>
      <DirPropsInnerContent>
        <PropertyGroup><BuildNum>0</BuildNum></PropertyGroup>
        <Import Project="BuildNum.xml" Condition="Exists('BuildNum.xml')"/>
      </DirPropsInnerContent>
      <DirPropsContent>
        <Project>$(DirPropsInnerContent)</Project>
      </DirPropsContent>
    </PropertyGroup>

    <!-- Read existing -->
    <ReadLinesFromFile Condition="$(DirPropsExists)" File="$(DirPropsPath)" ContinueOnError="True" >
      <Output TaskParameter="Lines" PropertyName="DirPropsReadOut" />
    </ReadLinesFromFile>

    <!-- Report missing content -->
    <Error Text="Project tag should contain: $(DirPropsInnerContent)" File="$(DirPropsPath)" Code="JJ-AUTO-INC"
           Condition="
           $(DirPropsExists) And $(BuildNumWasMissing) And 
           !$(DirPropsReadOut.Contains($(DirPropsInnerContent)))" />

    <!-- Auto-create -->
    <Message Condition="$(BuildNumWasMissing) And !$(DirPropsExists)" Text="JJ-AUTO-INC INIT => $(DirPropsPath)" Importance="High" />
    <!-- 
    NOTE: Mitigate file contention race condition:
    Overwrite prevents duplicate content while ContinueOnError should skip when file in use.
    -->
    <WriteLinesToFile Condition="$(BuildNumWasMissing) And !$(DirPropsExists)" 
                      File="$(DirPropsPath)" Lines="$(DirPropsContent)" 
                      Overwrite="True" ContinueOnError="True" />
  </Target>
  
  <!-- LOG -->
  
  <Target Name="JJ_AutoInc_Log" AfterTargets="$(BeforeTargets)">
    
    <!-- Content -->
    
    <ReadLinesFromFile File="$(XmlPath)" Condition="Exists('$(XmlPath)')" ContinueOnError="True">
      <Output TaskParameter="Lines" PropertyName="XmlReadOut" />
    </ReadLinesFromFile>
    
    <!-- Variables -->
    
    <PropertyGroup>
    <VariablesMessage>JJ-AUTO-INC VARS
 BuildNumWasMissing = $(BuildNumWasMissing)
 BuildNumWasZero = $(BuildNumWasZero)
 BuildNum = $(BuildNum)
 BuildNumWasFromXmljj = $(BuildNumWasFromXmljj)
 BeforeTargets = $(BeforeTargets)
 ProjDir = $(ProjDir)
 PackDir = $(PackDir)
 XmlPath = $(XmlPath)
 XmlReadOut = $(XmlReadOut)
 DirPropsPath = $(DirPropsPath)
 DirPropsExists = $(DirPropsExists)
 DirPropsReadOut = $(DirPropsReadOut)
 Dll = $(Dll)
 DllProd = $(DllProd)
 DllDev = $(DllDev)
    </VariablesMessage>
    </PropertyGroup>
    <Message Text="$(VariablesMessage)" Importance="High" />
  </Target>

  <!-- SAVE -->
    
  <Target Name="JJ_AutoInc_Save" AfterTargets="PostBuildEvent" Condition="!$(BuildNumWasZero) Or !Exists($(XmlPath))">

    <PropertyGroup>
      <!-- BuildNumNew may avoid some mid-build dirtying of the BuildNum variable. -->
      <BuildNumNew>$(BuildNum)</BuildNumNew>
      
      <!-- Start with 0 -->
      <BuildNumNew Condition="'$(BuildNumNew)'==''">0</BuildNumNew> 
      
      <!-- Increment -->
      <BuildNumNew>$([MSBuild]::Add($(BuildNumNew), 1))</BuildNumNew> 
      
      <!-- Build new content -->
      <XmlContent>
        <Project>
          <PropertyGroup>
            <BuildNum>$(BuildNumNew)</BuildNum>
            <DisableFastUpToDateCheck>True</DisableFastUpToDateCheck>
            <BuildNumWasFromXmljj>True</BuildNumWasFromXmljj>
          </PropertyGroup>
        </Project>
      </XmlContent>
    </PropertyGroup>
    
    <Message Text="JJ-AUTO-INC SAVE $(BuildNumNew) => .xml '$(XmlPath)'" Importance="High" />

    <!-- Save updated xml -->
    <WriteLinesToFile 
      Condition="'$(Dll)'==''" File="$(XmlPath)" Lines="$(XmlContent)"
      Overwrite="True" ContinueOnError="True" WriteOnlyWhenDifferent="True" />
    
    <!-- Use variant that updates time stamp again. Otherwise git confused. -->
    <!--<WriteLinesNoTimestamp--> 
    <WriteLinesToFile 
      Condition="'$(Dll)'!=''" File="$(XmlPath)" Lines="$(XmlContent)"
      Overwrite="True" ContinueOnError="True" WriteOnlyWhenDifferent="True" />
  </Target>
</Project>